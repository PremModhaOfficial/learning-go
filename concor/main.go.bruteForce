package main

import "fmt"

func main() {
	for i := 1; i <= 100; i++ {
		fmt.Println(i, fib(i))
	}
}

func fib(number int) int {
	baseCase := map[int]int{
		1: 1,
		2: 1,
	}

	if number < 2 {
		return baseCase[number]
	}

	return fib(number-1) + fib(number-2)
}
